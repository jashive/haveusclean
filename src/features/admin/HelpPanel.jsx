import React, { useEffect, useRef } from "react";
import { WORKSPACE_HELP } from "../../lib/serviceosLearnability.js";

export default function HelpPanel({ workspace, onClose, onStartTour }) {
  const panelRef = useRef(null); const help = WORKSPACE_HELP[workspace] || WORKSPACE_HELP["flight-control"];
  useEffect(() => { panelRef.current?.focus(); const key = (event) => { if (event.key === "Escape") onClose(); if (event.key !== "Tab") return; const nodes = panelRef.current?.querySelectorAll('button,[href],[tabindex]:not([tabindex="-1"])'); if (!nodes?.length) return; const first = nodes[0], last = nodes[nodes.length - 1]; if (event.shiftKey && document.activeElement === first) { event.preventDefault(); last.focus(); } else if (!event.shiftKey && document.activeElement === last) { event.preventDefault(); first.focus(); } }; window.addEventListener("keydown", key); return () => window.removeEventListener("keydown", key); }, [onClose]);
  return <div className="serviceos-help-backdrop" onMouseDown={(event) => event.target === event.currentTarget && onClose()}><aside ref={panelRef} className="serviceos-help-panel" role="dialog" aria-modal="true" aria-labelledby="serviceos-help-title" tabIndex={-1}><header><div><p className="admin-eyebrow">Help & operating guide</p><h2 id="serviceos-help-title">{help.title}</h2></div><button className="huc-button huc-button--secondary" onClick={onClose} aria-label="Close help">Close</button></header><section><h3>Standard operating flow</h3><ol>{help.steps.map((item) => <li key={item}>{item}</li>)}</ol></section><section><h3>Keyboard navigation</h3><p>Use Tab and Shift+Tab to move through controls. Press Enter or Space to activate a focused action. Press Escape to close help or the tour.</p></section><footer><button className="huc-button" onClick={onStartTour}>Start guided tour</button><p>The tour can be replayed anytime from this Help panel.</p></footer></aside></div>;
}
